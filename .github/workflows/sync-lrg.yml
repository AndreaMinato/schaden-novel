name: sync-lrg

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 1'

permissions:
  contents: write

concurrency:
  group: novel-sync
  cancel-in-progress: false

jobs:
  sync:
    uses: ./.github/workflows/sync-novel.yml
    with:
      slug: lrg
