name: sync-mga

on:
  workflow_dispatch:
    inputs:
      notify_test:
        description: Send a Telegram test message
        type: boolean
        default: false
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: write

concurrency:
  group: novel-sync
  cancel-in-progress: false

jobs:
  sync:
    uses: ./.github/workflows/sync-novel.yml
    secrets: inherit
    with:
      notify_test: ${{ inputs.notify_test || false }}
      slug: mga
