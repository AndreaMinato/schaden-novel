name: sync-rtw

on:
  workflow_dispatch:
  schedule:
    - cron: '0 10 * * *'

permissions:
  contents: write

concurrency:
  group: novel-sync
  cancel-in-progress: false

jobs:
  sync:
    uses: ./.github/workflows/sync-novel.yml
    with:
      slug: rtw
