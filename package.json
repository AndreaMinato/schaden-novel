{
  "name": "schaden-novel",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "import:all": "node ./scripts/import.mjs",
    "import:docs": "node ./scripts/import_ids.mjs",
    "deploy": "netlify deploy --prod --dir=dist",
    "git:sync": "git add . && git commit -m 'update' && git push && git checkout .",
    "publish": "pnpm import:docs && pnpm run deploy && pnpm git:sync"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.7",
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.14.5"
  },
  "devDependencies": {
    "cheerio": "^1.1.2"
  }
}
